---
name: Configure Fileserver
hosts: fileserver
become: true
tasks:
  - name: Update apt cache
    ansible.builtin.apt:
      update_cache: true

  - name: Show done message
    ansible.builtin.debug:
      msg: "=== Fileserver Updated! =="